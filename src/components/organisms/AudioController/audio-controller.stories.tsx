import * as React from 'react';
import { storiesOf } from '@storybook/react';
import AudioController from '.';

storiesOf('organisms/AudioController', module).add('audio controller', () => <AudioController />);
